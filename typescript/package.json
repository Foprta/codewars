{
  "name": "codewars",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "./node_modules/.bin/ts-mocha -p ./tsconfig.json **/*.spec.ts",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "watch:build": "tsc --watch",
    "watch:test": "npm test -- --watch --watch-extensions ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/expect": "^1.20.4",
    "@types/mocha": "^5.2.6",
    "@types/node": "^14.14.31",
    "eslint": "^7.20.0",
    "eslint-config-google": "^0.14.0",
    "mocha": "^6.1.4",
    "rimraf": "^2.6.3",
    "ts-mocha": "^6.0.0",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
